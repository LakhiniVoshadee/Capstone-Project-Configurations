spring:
  mvc:
    problemdetails:
      enabled: true

  datasource:
    url: jdbc:mysql://136.119.143.88:3306/enrollment_db?createDatabaseIfNotExist=true&useSSL=true&trustServerCertificate=true&allowPublicKeyRetrieval=true
    username: mysql
    password: '-~zm=m6~8n$,Y\dJ'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
#        format_sql: true

server:
  port: 0

eureka:
  instance:
    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
